<div class='grid'>
  {{#each posts as |post| }}
  <div class='card'>
    <p>{{format_date post.date_created}}</p>
    <h3><a href='/api/posts/{{post.id}}'>{{post.title}}</a></h3>
  </div>
  {{/each}}
</div>