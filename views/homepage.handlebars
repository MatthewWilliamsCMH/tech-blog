{{#each posts as |post| }}
<div class="row mb-4 project" >
  <div class="col-md-5">
    <p>
      <a href="/post/{{post.id}}">{{post.title}}</a>
    </p>
    <p>Created by {{post.user.username}} on {{format_date post.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.content}} <!--not sure yet what this is supposed to be.-->
    </p>
  </div>
</div>
{{/each}}